import userRegistrar from '../../../../src/web/modules/userRegistrar';

describe('User Registrar', () => {
  describe('register()', () => {
    it('creates a user');
    it('throws an error');
  });
});
